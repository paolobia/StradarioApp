# Build output
bin/
obj/
publish/

# User-specific files
*.user
*.suo
.vs/

# NuGet
*.nupkg
*.snupkg

# OS files
.DS_Store
Thumbs.db

# Local Claude Code settings
.claude/settings.local.json

# Large runtime data file (fetched separately, see CLAUDE.md)
cities500.csv

# Personal sample/test data
*.kml
*.gpx
*.stradario

# Debug-only diagnostic log (Services/DebugLog.cs)
debug.log

# osm/ (OsmExtractor, vedi osm/CLAUDE.md): dati scaricati/generati, fino a
# centinaia di GB (.osm.pbf per continente, CSV estratti, zip di release) —
# solo il codice sorgente di OsmExtractor va in git, mai questi dati
osm/osm_data/
osm/OsmExtractor/extract_all.log
